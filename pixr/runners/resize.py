from pixr.runners import BaseRunner


class ResizeRunner(BaseRunner):
    def run(self) -> None:
        """Resize image to specific dimensions."""
        print("Command 'resize' is not yet implemented.")
